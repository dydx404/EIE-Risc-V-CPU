# Tools (Build + CI Scripts)

Utility scripts for building, testing, and CI.

Subfolders:
- `scripts/` — helper scripts (hex gen, run tests, compile C prog)
- `ci/`      — GitHub Actions workflows (Verilator build, tests)

Goal: automate build + test workflow.
