# CPU Extensions

Optional components beyond base RV32I.

Subfolders:
- `muldiv/` — RV32M extension (mul/div instructions)
- `csr/`    — control and status registers
- `mmio/`   — memory-mapped I/O (e.g., Vbuddy)

Goal: extend the ISA or support extra peripherals.
